services:
  # Static site — Next.js static export served from ./out (free tier friendly).
  - type: web
    name: jogesh-portfolio
    runtime: static
    rootDir: jogesh-portfolio
    buildCommand: npm ci && npm run build
    staticPublishPath: ./out
    autoDeploy: true
